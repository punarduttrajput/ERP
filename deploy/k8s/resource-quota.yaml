apiVersion: v1
kind: ResourceQuota
metadata:
  name: erp-platform-quota
  namespace: erp-platform
spec:
  hard:
    requests.cpu: "20"
    requests.memory: 40Gi
    limits.cpu: "40"
    limits.memory: 80Gi
    persistentvolumeclaims: "10"
    services: "20"
    pods: "100"
